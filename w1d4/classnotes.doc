Forms in Bootstrap

create-react-app pet Forms

import React, { useState } from  'react';
    
    
const UserForm = (props) => {
    const [username, setUsername] = useState("");
    const [email, setEmail] = useState("");
    const [password, setPassword] = useState("");  
    
    const createUser = (e) => {
    e.preventDefault();
    const newUser = { username, email, password };
    console.log("Welcome", newUser);
    };
    
    return(
    <form onSubmit={ createUser }>
    <div>
    <label>Username: </label> 
    <input type="text" onChange={ (e) => setUsername(e.target.value) } />
    </div>
    <div>
                <label>Email Address: </label> 
                <input type="text" onChange={ (e) => setEmail(e.target.value) } />
            </div>
    <div>
                <label>Password: </label>
                <input type="text" onChange={ (e) => setPassword(e.target.value) } />
            </div>
    <input type="submit" value="Create User" />
    </form>
    );
};
    
export default UserForm;


create a function folder called Petform.jsx

const PetForm + (props) => {
    return(
        div
        h1 Make a grooming appointment
        div
        form action = ""
        label-- Name, Age, hair length
        input type submit it
        div
    )
}

export default PetForm

dynamically show information on the form by using useState
pudate the state variable to be whatever is in the input onChange event
setName(e.target.value) is the input -- the target of the event

Pet Name: {name}
step 1: let [age, setAge] = useState(null)
step 2: onChange event  inside input tag {(e) => setAge(e.target.value)}
step 3: Age: {age}

let [breed, setBreed] = useState(null)
{(e) => setBreed(e.target.value)}
Breed: {breed}

put value in value for dropdown menu

Conditional rendering
render something on the PetForm
now we gotta talk about ternary operator {
    name.length < 2?
    return p name must be greater than 2 p:""
}
ternary operator

